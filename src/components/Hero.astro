---
import Form from './Form.astro';
---

<div class='section-hero'>
  <div class='hero'>
    <div class='hero-text-box'>
      <div class='header__title'>
        <h1>
          LEAVE THE
          <!-- highlight effect -->
          <span class='highlight'>JUNK</span>
          TO US <br />
          <span class='highlight'>SAN JOSE</span>
        </h1>

        <p>We handle the heavy lifting, so you don't have to!</p>
        <button class='btn btn--text btn--scroll-to'
          >Learn more &DownArrow;</button
        >
      </div>
    </div>
    <Form />
  </div>
</div>

<style lang='scss' is:global>
  // @use '../styles/scss/base/font' as *;

  .section-hero {
    min-height: 70vh;
    position: relative;
    // background-color: transparent;
    outline: solid 3px #fdf2e9;
    padding: max(4rem, 15vh) 0;
  }
  .hero {
    /* 2080px */
    max-width: 130rem;
    margin: 0 auto;
    padding: 2rem 8rem;
    display: grid;
    grid-template-columns: minmax(300px, 1fr) minmax(350px, 1fr);
    gap: 2rem;
    place-items: center;
  }

  .hero-text-box {
    // height: 80vh;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .header__title {
    flex: 1;
    display: grid;
    row-gap: 4rem;
    align-content: center;
    justify-content: center;

    align-items: start;
    justify-items: start;
  }

  .highlight {
    position: relative;
    z-index: 1;
  }

  .highlight::after {
    display: block;
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    height: 100%;
    width: 100%;
    z-index: -1;
    opacity: 0.6;
    transform: scale(1.07, 1.05) skewX(-15deg);
    background-image: var(--gradient-primary);
  }

  .btn {
    display: inline-block;
    background-color: black;
    // background-color: var(--clr-primary-200);
    font-size: 1.6rem;
    font-family: inherit;
    font-weight: 500;
    border: none;
    padding: 1.25rem 4.5rem;
    border-radius: 10rem;
    cursor: pointer;
    transition: all 0.3s;
  }
  @media (max-width: 59em) {
    .hero {
      padding: 1rem 0.5rem;
      grid-template-columns: minmax(250px, 1fr) minmax(250px, 1fr);
      gap: 0;
    }

    .header__title {
      flex: 1;
      display: grid;
      padding: 0 2rem;
      row-gap: 2rem;
    }

    .hero-text-box {
      background-color: rgba(245, 224, 210, 0.28);
    }
  }

  /* BELOW 704px (Smaller tablets) */
  @media (max-width: 44em) {
    .section-hero {
      min-height: 60vh;
      // padding: max(3rem, 5vh) 0;
    }
    .hero {
      padding: 1rem 2.5rem 3rem 2.5rem;
      gap: 1.5rem;
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 34em) {
    .hero {
      padding: 1rem 0 2.5rem 1rem;
      gap: 1.1rem;
      grid-template-columns: 1fr;
    }
  }
</style>
